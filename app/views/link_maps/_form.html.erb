<%= form_with(model: link_map, local: true) do |form| %>
  <% if link_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link_map.errors.count, "error") %> prohibited this link_map from being saved:</h2>

      <ul>
        <% link_map.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :original_url %>
    <%= form.text_field :original_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
